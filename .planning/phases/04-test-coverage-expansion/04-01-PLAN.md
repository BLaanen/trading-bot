---
phase: 04-test-coverage-expansion
plan: 1
title: Reconciliation Failures & Empty DataFrame Tests
type: execute
wave: 1
depends_on: []
files_modified:
  - test_reconcile.py
autonomous: true

must_haves:
  truths:
    - "test_reconcile.py runs to completion with 0 failures and covers: API failure, stale local removal, missing local addition, qty/price drift correction, cash reclamation, empty scanner data"
    - "When reconcile_with_broker cannot reach Alpaca, it returns False and no local state is modified"
    - "When broker has a position not in local state, reconciliation adds it with strategy='UNKNOWN_RECONCILED'"
    - "When local has a position the broker doesn't, reconciliation removes it and reclaims cash"
    - "When broker qty differs from local qty, reconciliation corrects local to match broker"
    - "Scanner fetch_data returns None for insufficient data and scanner functions handle None gracefully"
  artifacts:
    - path: test_reconcile.py
      provides: "Standalone test script for reconciliation and empty DataFrame edge cases"
      contains: "def check(name, condition)"
  key_links:
    - from: test_reconcile.py
      to: reconcile.py
      via: "from reconcile import reconcile_with_broker"
    - from: test_reconcile.py
      to: scanner.py
      via: "from scanner import fetch_data"
---

<objective>
Create test_reconcile.py covering reconciliation failure paths and empty DataFrame handling in the scanner. These are the two most critical untested areas — reconciliation guards all trading, and empty DataFrames can crash scanner strategies silently. Note: trailing stop broker sync (the 3rd area in the phase goal) is already covered by test_executor.py tests 10-18 (9 tests covering replace success/failure, double failure, no order ID, disabled, simulated, multi-position, and exit-skip scenarios). No additional trailing stop tests needed.
</objective>

<context>
@reconcile.py — full source, all paths to test
@scanner.py — fetch_data and strategy functions, lines 69-90 for data validation
@test_executor.py — test infrastructure pattern to replicate (sandbox, check(), MagicMock)
@.planning/codebase/TESTING.md — conventions
@risk_manager.py — Position, PortfolioState, load_positions, save_positions
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create test_reconcile.py scaffold with sandbox and helpers</name>
  <files>test_reconcile.py</files>
  <read_first>test_executor.py (lines 1-68 for sandbox/check pattern), reconcile.py (full file), risk_manager.py (Position and PortfolioState dataclass fields)</read_first>
  <action>
Create test_reconcile.py at project root with:

1. Sandbox setup (identical pattern to test_executor.py lines 1-22):
   - tempfile.mkdtemp with prefix="trading_test_reconcile_"
   - Set TRADING_STATE_DIR env var
   - atexit cleanup

2. Imports: config.AgentConfig, risk_manager.Position/PortfolioState/load_positions/save_positions, reconcile.reconcile_with_broker, unittest.mock.MagicMock/patch, scanner.fetch_data

3. check() function (same as test_executor.py lines 60-67)

4. _reset_state(cash=10000) helper (same as test_executor.py lines 42-49)

5. _make_position() helper:
```python
def _make_position(ticker="TEST", shares=10, entry=100.0, current=105.0,
                   stop=95.0, target=110.0, strategy="PULLBACK"):
    return Position(
        ticker=ticker, shares=shares, entry_price=entry,
        current_price=current, stop_loss=stop, initial_stop=stop,
        target=target, strategy=strategy,
        entry_date="2026-04-10", high_water_mark=current,
    )
```

6. _make_broker_position() helper returning a MagicMock with .symbol, .qty, .avg_entry_price, .current_price, .market_value attributes

7. Header print block: "RECONCILIATION & DATAFRAME TESTS"
  </action>
  <verify>python -c "import test_reconcile" succeeds without errors</verify>
  <done>test_reconcile.py exists, contains "def check(name, condition)", contains "def _make_position", contains "def _make_broker_position", contains "TRADING_STATE_DIR"</done>
</task>

<task type="auto">
  <name>Task 2: Reconciliation failure path tests</name>
  <files>test_reconcile.py</files>
  <read_first>reconcile.py (lines 35-60 for API failure path, lines 46-49 for credential check), test_reconcile.py (current state from Task 1)</read_first>
  <action>
Add 4 test sections to test_reconcile.py:

**Test 1: API unreachable returns False, no state change**
- _reset_state with 1 position via save_positions
- Patch alpaca_trade_api.REST to raise ConnectionError on instantiation
- Set ALPACA_API_KEY and ALPACA_API_SECRET env vars to "test_key"/"test_secret"
- Call reconcile_with_broker(AgentConfig())
- check result is False
- check load_positions().positions still has 1 position (unchanged)

**Test 2: Missing credentials skips reconciliation, returns True**
- Save original env vars, delete ALPACA_API_KEY and ALPACA_API_SECRET
- Call reconcile_with_broker(AgentConfig())
- check result is True
- Restore original env vars

**Test 3: Broker has position not in local → adds with UNKNOWN_RECONCILED**
- _reset_state with empty positions list
- Mock client.list_positions() to return [_make_broker_position("AAPL", 50, 150.0, 155.0)]
- Mock client.get_account() to return MagicMock(equity="10000")
- Patch alpaca_trade_api.REST to return mock client
- Set env vars for credentials
- Call reconcile_with_broker(AgentConfig())
- check result is True
- check load_positions() has 1 position with ticker="AAPL", shares=50, strategy="UNKNOWN_RECONCILED"

**Test 4: Local has position broker doesn't → removes and reclaims cash**
- _reset_state(cash=5000) with 1 position: ticker="GONE", shares=10, current_price=100.0
- Mock client.list_positions() to return [] (empty)
- Mock client.get_account()
- Patch alpaca_trade_api.REST
- Call reconcile_with_broker(AgentConfig())
- check result is True
- check load_positions().positions is empty
- check load_positions().cash == 6000.0 (5000 + 10 * 100.0 reclaimed)
  </action>
  <verify>Run test_reconcile.py, tests 1-4 print [PASS]</verify>
  <done>test_reconcile.py contains "Test 1: API unreachable", "Test 2: Missing credentials", "Test 3: Broker position added", "Test 4: Local position removed". All 4 tests produce [PASS].</done>
</task>

<task type="auto">
  <name>Task 3: Reconciliation drift correction tests</name>
  <files>test_reconcile.py</files>
  <read_first>reconcile.py (lines 109-123 for qty/price drift logic), test_reconcile.py (current state)</read_first>
  <action>
Add 2 test sections:

**Test 5: Qty mismatch → local corrected to broker qty**
- _reset_state with position: ticker="DRIFT", shares=10, entry=100.0
- Mock broker: _make_broker_position("DRIFT", 15, 100.0, 105.0) — broker says 15 shares
- Patch REST, call reconcile_with_broker
- check result is True
- check load_positions().positions[0].shares == 15

**Test 6: Entry price drift > $0.50 → corrected**
- _reset_state with position: ticker="PRICE", shares=10, entry=100.0
- Mock broker: _make_broker_position("PRICE", 10, 101.50, 105.0) — broker says $101.50 entry (drift > $0.50)
- Patch REST, call reconcile_with_broker
- check result is True
- check abs(load_positions().positions[0].entry_price - 101.50) < 0.01

Add final summary print at end of file: `print(f"\n{'='*70}\n  {passed} passed, {failed} failed\n{'='*70}")`
  </action>
  <verify>Run test_reconcile.py, tests 5-6 print [PASS], summary shows 0 failed</verify>
  <done>test_reconcile.py contains "Test 5: Qty mismatch", "Test 6: Entry price drift". Running `python test_reconcile.py` shows 0 failed.</done>
</task>

<task type="auto">
  <name>Task 4: Empty DataFrame and scanner edge case tests</name>
  <files>test_reconcile.py</files>
  <read_first>scanner.py (lines 64-90 for fetch_data, _provider, get_provider; lines 184, 258, 333, 409, 486 for None checks in strategy functions), test_reconcile.py (current state)</read_first>
  <action>
Add scanner edge case tests BEFORE the summary print block:

**Test 7: fetch_data returns None for empty provider response**
- Patch data_provider.get_provider to return a mock provider where get_bars() returns an empty DataFrame (pd.DataFrame())
- Call fetch_data("FAKE")
- check result is None

**Test 8: fetch_data returns None for insufficient data (< 60 bars)**
- Mock provider.get_bars to return a DataFrame with only 30 rows of OHLCV data
- Call fetch_data("SHORT")
- check result is None

**Test 9: Scanner strategy functions handle None data gracefully**
- Scanner functions (scan_pullback, scan_consolidation_breakout, scan_ma_bounce, scan_sector_momentum, scan_powerx) take `(ticker: str, config: AgentConfig)` and call fetch_data internally
- Patch `scanner.fetch_data` to return None (simulating no data available)
- For each scanner function, call with ("FAKE", AgentConfig())
- check each returns None (no crash) — these functions guard with `if data is None:` early returns
- Import the scan functions from scanner.py (check actual exported names — may be scan_pullback, scan_consolidation_breakout, scan_ma_bounce, scan_sector_momentum, scan_powerx)

Move the summary print block to after Test 9.
  </action>
  <verify>Run test_reconcile.py, all 9 tests pass with 0 failures</verify>
  <done>test_reconcile.py contains "Test 7: fetch_data empty", "Test 8: fetch_data insufficient", "Test 9: Scanner handles None". Running `python test_reconcile.py` shows 0 failed.</done>
</task>
</tasks>

<verification>
python test_reconcile.py 2>&1 | tail -5
# Expected: "N passed, 0 failed"
</verification>

<success_criteria>
- test_reconcile.py runs to completion with 0 failures
- API failure path tested (returns False, state unchanged)
- Missing credentials path tested (returns True, skips)
- Broker-only position addition tested (UNKNOWN_RECONCILED strategy)
- Local-only position removal tested (cash reclaimed)
- Qty and price drift correction tested
- Empty/insufficient DataFrame handling tested
- Scanner functions don't crash on None input
</success_criteria>

<output>.planning/phases/04-test-coverage-expansion/SUMMARY.md</output>
