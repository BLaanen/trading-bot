---
phase: 04-test-coverage-expansion
plan: 2
title: Regime Detection & Pipeline Filtering Tests
type: execute
wave: 1
depends_on: []
files_modified:
  - test_regime.py
autonomous: true

must_haves:
  truths:
    - "test_regime.py runs to completion with 0 failures and covers: RegimeState properties for all 3 regimes, detect_regime fallback, step_filter regime filtering, max_trades capping"
    - "RegimeState.allowed_strategies returns correct strategy list for TRENDING_UP (5 strategies), SIDEWAYS (2), and TRENDING_DOWN (2)"
    - "RegimeState.max_positions returns 6 for TRENDING_UP, 3 for SIDEWAYS, 2 for TRENDING_DOWN"
    - "RegimeState.position_size_mult returns 1.0 for TRENDING_UP, 0.5 for SIDEWAYS, 0.25 for TRENDING_DOWN"
    - "detect_regime returns SIDEWAYS with confidence 0.3 when SPY data is unavailable or too short"
    - "step_filter removes signals whose strategy is not in regime.allowed_strategies"
    - "max_trades calculation caps new entries when positions approach regime.max_positions"
    - "[review] position_size_mult boundary tests use values that satisfy strict > inequality (confidence=0.85/breadth=75, NOT 0.8/70)"
    - "[review] detect_regime mock patches regime.get_provider (module-level import), not data_provider.get_provider"
  artifacts:
    - path: test_regime.py
      provides: "Standalone test script for regime detection and orchestrator filtering"
      contains: "def check(name, condition)"
  key_links:
    - from: test_regime.py
      to: regime.py
      via: "from regime import Regime, RegimeState, detect_regime"
    - from: test_regime.py
      to: orchestrator.py
      via: "from orchestrator import step_filter"
---

<objective>
Create test_regime.py covering regime detection edge cases and how regime state flows through the orchestrator's filtering and position-capping logic. Regime detection is the system's most important safety mechanism — it determines whether to trade at all — yet has zero test coverage.
</objective>

<context>
@regime.py — full source: Regime enum, RegimeState dataclass, detect_regime function
@orchestrator.py — step_filter (lines 100-134), max_trades calculation (lines 358-363)
@test_executor.py — test infrastructure pattern (sandbox, check(), MagicMock)
@.planning/codebase/TESTING.md — conventions
@scanner.py — Signal dataclass for constructing test signals
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create test_regime.py scaffold with helpers</name>
  <files>test_regime.py</files>
  <read_first>test_executor.py (lines 1-68 for sandbox/check pattern), regime.py (lines 35-105 for Regime enum and RegimeState), scanner.py (Signal dataclass fields)</read_first>
  <action>
Create test_regime.py at project root with:

1. Sandbox setup (same pattern as test_executor.py):
   - tempfile.mkdtemp(prefix="trading_test_regime_")
   - Set TRADING_STATE_DIR env var
   - atexit cleanup

2. Imports: regime.Regime/RegimeState/detect_regime, orchestrator.step_filter, config.AgentConfig, scanner.Signal, risk_manager.PortfolioState/Position/load_positions/save_positions, unittest.mock.MagicMock/patch

3. check() function, passed/failed counters

4. _reset_state(cash=10000) helper

5. _make_regime() helper:
```python
def _make_regime(regime_type=Regime.TRENDING_UP, confidence=0.8,
                 spy_vs_200=5.0, breadth=70.0, vol_pct=40.0,
                 mom_20d=3.0, mom_60d=5.0, golden_cross=True):
    return RegimeState(
        regime=regime_type, confidence=confidence,
        spy_vs_200=spy_vs_200, breadth=breadth,
        volatility_percentile=vol_pct,
        momentum_20d=mom_20d, momentum_60d=mom_60d,
        golden_cross=golden_cross,
    )
```

6. _make_signal() helper (same as test_executor.py lines 52-57)

7. Header: "REGIME DETECTION & PIPELINE FILTERING TESTS"
  </action>
  <verify>python -c "import test_regime" succeeds without errors</verify>
  <done>test_regime.py exists, contains "def _make_regime", "def _make_signal", "def check(name, condition)", "TRADING_STATE_DIR"</done>
</task>

<task type="auto">
  <name>Task 2: RegimeState property tests for all 3 regimes</name>
  <files>test_regime.py</files>
  <read_first>regime.py (lines 57-104 for position_size_mult, allowed_strategies, max_positions properties), test_regime.py (current state)</read_first>
  <action>
Add 3 test sections:

**Test 1: TRENDING_UP regime properties**
- regime = _make_regime(Regime.TRENDING_UP, confidence=0.85, breadth=75.0)
- check allowed_strategies == ["PULLBACK", "BREAKOUT", "MA_BOUNCE", "SECTOR_MOMENTUM", "POWERX"]
- check max_positions == 6
- check position_size_mult == 1.25 (confidence > 0.8 AND breadth > 70 — strict inequality)
- Also test with confidence=0.6: position_size_mult should be 1.0

**Test 2: SIDEWAYS regime properties**
- regime = _make_regime(Regime.SIDEWAYS)
- check allowed_strategies == ["PULLBACK", "POWERX"]
- check max_positions == 3
- check position_size_mult == 0.5

**Test 3: TRENDING_DOWN regime properties**
- regime = _make_regime(Regime.TRENDING_DOWN)
- check allowed_strategies == ["SECTOR_MOMENTUM", "POWERX"]
- check max_positions == 2
- check position_size_mult == 0.25
  </action>
  <verify>Run test_regime.py, tests 1-3 all [PASS]</verify>
  <done>test_regime.py contains "Test 1: TRENDING_UP", "Test 2: SIDEWAYS", "Test 3: TRENDING_DOWN". All produce [PASS].</done>
</task>

<task type="auto">
  <name>Task 3: detect_regime fallback and step_filter tests</name>
  <files>test_regime.py</files>
  <read_first>regime.py (lines 107-119 for detect_regime fallback when SPY data unavailable), orchestrator.py (lines 100-120 for step_filter regime filtering logic), test_regime.py (current state)</read_first>
  <action>
Add 3 test sections:

**Test 4: detect_regime falls back to SIDEWAYS when no SPY data**
- Patch regime.get_provider (module-level import target) to return a mock where get_bars("SPY", period="2y") returns None
- Call detect_regime(AgentConfig())
- check result.regime == Regime.SIDEWAYS
- check result.confidence == 0.3
- check result.breadth == 50

**Test 5: detect_regime falls back to SIDEWAYS when SPY data too short**
- Mock get_bars to return a DataFrame with only 100 rows (< 200 required)
- Call detect_regime(AgentConfig())
- check result.regime == Regime.SIDEWAYS
- check result.confidence == 0.3

**Test 6: step_filter removes signals not allowed in current regime**
- Create 4 signals: PULLBACK, BREAKOUT, POWERX, MA_BOUNCE
- Create SIDEWAYS regime (allowed: PULLBACK, POWERX only)
- Call step_filter(signals, approved={}, config=AgentConfig(), regime=regime)
  where approved is {"PULLBACK": ["*"], "BREAKOUT": ["*"], "POWERX": ["*"], "MA_BOUNCE": ["*"]}
  Note: check step_filter signature — if paper_exploration_mode affects validation, set config.paper_exploration_mode = True to skip validation layer and isolate regime filtering
- check result has exactly 2 signals
- check result signals are PULLBACK and POWERX only
  </action>
  <verify>Run test_regime.py, tests 4-6 all [PASS]</verify>
  <done>test_regime.py contains "Test 4: detect_regime no data", "Test 5: detect_regime short data", "Test 6: step_filter regime". All produce [PASS].</done>
</task>

<task type="auto">
  <name>Task 4: Max trades capping and regime-None tests</name>
  <files>test_regime.py</files>
  <read_first>orchestrator.py (lines 358-367 for max_trades calculation and regime=None in step_filter), regime.py (lines 96-104 for max_positions), config.py (exploration_max_positions field and default value), test_regime.py (current state)</read_first>
  <action>
Add 3 test sections:

**Test 7: max_trades capped by regime.max_positions minus current positions**
- This tests the LOGIC from orchestrator.py line 363: max_trades = min(ceiling, regime.max_positions - len(positions))
- Don't call full pipeline — test the arithmetic directly:
  - SIDEWAYS regime: max_positions = 3
  - Current positions: 2
  - ceiling (paper mode) = min(config.exploration_max_positions, regime.max_positions * 2) = min(6, 6) = 6
  - max_trades = min(6, 3 - 2) = 1
  - check max_trades == 1
  - Test with 3 current positions: max_trades = min(6, 3 - 3) = 0
  - check max_trades == 0 (no new trades allowed)

**Test 8: step_filter with regime=None skips regime filtering**
- Create signals with strategies PULLBACK and BREAKOUT
- Call step_filter(signals, approved={"PULLBACK": ["*"], "BREAKOUT": ["*"]}, config=AgentConfig(paper_exploration_mode=True), regime=None)
- check all signals pass through (regime filtering skipped per line 114: `if regime:`)

**Test 9: TRENDING_UP high-confidence position_size_mult boundary**
- _make_regime(Regime.TRENDING_UP, confidence=0.85, breadth=75.0) → check mult == 1.25 (both conditions met: >0.8 and >70)
- _make_regime(Regime.TRENDING_UP, confidence=0.80, breadth=75.0) → check mult == 1.0 (confidence NOT > 0.8, exactly at boundary)
- _make_regime(Regime.TRENDING_UP, confidence=0.85, breadth=70.0) → check mult == 1.0 (breadth NOT > 70, exactly at boundary)
- _make_regime(Regime.TRENDING_UP, confidence=0.7, breadth=80.0) → check mult == 1.0 (confidence below threshold)

Add final summary: `print(f"\n{'='*70}\n  {passed} passed, {failed} failed\n{'='*70}")`
  </action>
  <verify>Run test_regime.py, all 9 tests pass. Summary shows 0 failed.</verify>
  <done>test_regime.py contains "Test 7: max_trades capping", "Test 8: step_filter no regime", "Test 9: position_size_mult boundary". Running `python test_regime.py` shows 0 failed.</done>
</task>
</tasks>

<verification>
python test_regime.py 2>&1 | tail -5
# Expected: "N passed, 0 failed"
</verification>

<success_criteria>
- test_regime.py runs to completion with 0 failures
- All 3 regime types tested for allowed_strategies, max_positions, position_size_mult
- detect_regime fallback tested (None data, short data → SIDEWAYS)
- step_filter regime filtering tested (removes disallowed strategies)
- max_trades arithmetic tested at position limits
- regime=None path tested (no filtering)
- position_size_mult boundary conditions tested (confidence and breadth thresholds)
</success_criteria>

<output>.planning/phases/04-test-coverage-expansion/SUMMARY.md</output>
